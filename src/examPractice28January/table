CREATE TABLE StudentClass (
	id NUMBER GENERATED ALWAYS AS IDENTITY START WITH  1 INCREMENT BY 1 PRIMARY KEY,
    name VARCHAR2(100),
    sclass INTEGER,
    age INTEGER,
    classTeacher VARCHAR2(100),
    mark NUMBER(5, 2)
);

DROP TABLE StudentClass
SELECT * FROM StudentClass